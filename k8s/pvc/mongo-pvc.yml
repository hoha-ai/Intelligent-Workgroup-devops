apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: deeptalk-mongo-pvc
  namespace: deeptalk
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 16Gi
